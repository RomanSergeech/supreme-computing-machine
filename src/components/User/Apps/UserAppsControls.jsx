'use client';

import TableControls from '@/components/common/TableControls';

export default function UserAppsControls() {
  return (
    <TableControls
      search={null}
      showFilters={true}
      showExport={true}
    />
  );
}
